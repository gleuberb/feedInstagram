import React from 'react';//pra fazer funcionar as tags JSX (Java Script com XML = HTML)
import ReactDOM from 'react-dom';
import App from './App';
import './global.css'

ReactDOM.render(<App />, document.getElementById('root'));

